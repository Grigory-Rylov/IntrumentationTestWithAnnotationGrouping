apply plugin: 'groovy'

repositories {
    jcenter()
    mavenLocal()
    maven {
        url 'https://maven.google.com'
    }
}
dependencies {
    compile 'com.android.tools.build:gradle:3.0.1'

    compile 'com.github.grishberg:android-instrumental-test-runner:0.5.10'
}

dependencies {
    compile gradleApi()
}